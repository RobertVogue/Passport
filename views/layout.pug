doctype html
html
  head
      block head
        title="Passport Group Project"
        link(rel='stylesheet' href='/stylesheets/index.css')
  body
    header.container <!-- this is the navbar container -->
      div.navbar-registered
        div.logo
          a.logo(href="/") <!-- does this need to have a full url or just the path? -->
            img.logo-img(alt="Passports Logo" src="https://i.postimg.cc/mrMzWH9K/Pass-Port-Icon.png")
        div.nav-item-container
          div.nav-item1
            a.nav-item1.nav-link(type='/profile') My Stamps <!-- needs to be hidden if not logged in  -->
          div.nav-item2
            a.nav-item2.nav-link(href='/create-stamp') Create Stamp
          div.nav-item3
            a.nav-item3.nav-link(href='/register') Register
          div.nav-item4
            a.nav-item4.nav-link(href='/login') Login
    block content
    include footer